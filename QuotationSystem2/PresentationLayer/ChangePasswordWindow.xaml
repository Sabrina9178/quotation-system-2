<Window x:Class="QuotationSystem2.PresentationLayer.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuotationSystem2.PresentationLayer"
        xmlns:loc="clr-namespace:QuotationSystem2.PresentationLayer.Services.Localization"
        mc:Ignorable="d"
        Title="{loc:Loc Key=Change|Password, ForceStatic=True}" 
        Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <StackPanel Margin="20">
        <TextBlock Text="{loc:Loc Key=Old|Password, ForceStatic=True}" Margin="0,10,0,5"/>
        <PasswordBox x:Name="OldPasswordBox"/>

        <TextBlock Text="{loc:Loc Key=New|Password, ForceStatic=True}" Margin="0,15,0,5"/>
        <PasswordBox x:Name="NewPasswordBox"/>

        <TextBlock x:Name="PasswordConstraintTextBlock"
                   Foreground="Gray" FontSize="12" Margin="0,10,0,15"/>

        <Button Content="{loc:Loc Key=Change|Password, ForceStatic=True}"
                HorizontalAlignment="Right" Width="100"
                Click="ChangePasswordButton_Clicked"/>
    </StackPanel>
</Window>
